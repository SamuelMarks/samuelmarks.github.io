SequenceLength
==============

Produces a scalar(tensor of empty shape) containing the number of tensors in 'input_sequence'.

**Abstract Signature:**

``SequenceLength(input_sequence)``

*No implementations mapped.*