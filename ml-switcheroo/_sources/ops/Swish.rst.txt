Swish
=====

Swish function takes one input data (Tensor<T>) and produces one output data (Tensor<T>) of the same shape, where $Swish(x) = x * sigmoid(alpha * x)$.

**Abstract Signature:**

``Swish(X: Tensor, alpha: float)``

*No implementations mapped.*