ml_switcheroo.core.tikz.emitter
===============================

.. py:module:: ml_switcheroo.core.tikz.emitter

.. autoapi-nested-parse::

   TikZ Emitter Module.

   This module converts a language-agnostic ``LogicalGraph`` into renderable TikZ code.
   It acts as the backend of the visualization subsystem.

   Responsibilities:
   1.  **Layout**: Assigns (x, y) coordinates to nodes using a rank-based topological sort.
       Vertical layout is standard for neural networks (Input at top, Output at bottom).
   2.  **Conversion**: transforms abstract Logical components into Concrete Syntax Tree (CST)
       objects defined in ``ml_switcheroo.core.tikz.nodes``.
   3.  **Rendering**: Generates the final LaTeX string including ``\node`` tables
       for layer configuration.



Classes
-------

.. autoapisummary::

   ml_switcheroo.core.tikz.emitter.TikzEmitter


Module Contents
---------------

.. py:class:: TikzEmitter(y_spacing: float = 2.5, x_spacing: float = 3.0)

   Orchestrates the conversion of a LogicalGraph to TikZ source code.

   .. attribute:: y_spacing

      Vertical distance between ranks.

      :type: float

   .. attribute:: x_spacing

      Horizontal distance between sibling nodes.

      :type: float


   .. py:attribute:: y_spacing
      :value: 2.5



   .. py:attribute:: x_spacing
      :value: 3.0



   .. py:method:: emit(graph: ml_switcheroo.core.tikz.analyser.LogicalGraph) -> str

      Generates the TikZ source code string for the provided graph.

      :param graph: The logical graph (nodes and edges) extracted from analysis.

      :returns: A complete ``\begin{tikzpicture}...\end{tikzpicture}`` block.
      :rtype: str



